#!/bin/bash

function mypy {

	local dirname="$(dirname "$(realpath $1)")"
	local dotpyfile="$(basename "$1")"
	docker run --rm --volume "$dirname":"$dirname" --workdir "$dirname" --entrypoint "python" python "$dotpyfile"

}
