#!/bin/bash

function mypy {

	local dirname="$(dirname "$(realpath $1)")"
	local dotpyfile="$(basename "$1")"
	docker run --rm --interactive --tty --volume "$dirname":"$dirname" --workdir "$dirname" --entrypoint "python" python "$dotpyfile"

}
